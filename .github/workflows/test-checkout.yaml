apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Checkout only test

on:
  push:
    branches:
      - '**'

jobs:
  test-checkout:
    steps:
      - name: Checkout
        uses: cloudbees-io/checkout@v2
