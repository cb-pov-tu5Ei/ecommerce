<?xml version="1.0" encoding="UTF-8"?>
<!--
    Maven Settings Template for Artifactory Authentication

    This file serves as a reference template showing how Jenkins will authenticate
    to Artifactory during the deployment stage.

    In the Jenkins pipeline, credentials are injected as environment variables:
    - ARTIFACTORY_USERNAME
    - ARTIFACTORY_PASSWORD

    Maven resolves ${env.*} placeholders at runtime when this settings.xml is
    generated dynamically in the pipeline.

    Server IDs must match the repository IDs in pom.xml distributionManagement:
    - artifactory-releases
    - artifactory-snapshots
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              https://maven.apache.org/xsd/settings-1.0.0.xsd">
    <servers>
        <server>
            <id>artifactory-releases</id>
            <username>${env.ARTIFACTORY_USERNAME}</username>
            <password>${env.ARTIFACTORY_PASSWORD}</password>
        </server>
        <server>
            <id>artifactory-snapshots</id>
            <username>${env.ARTIFACTORY_USERNAME}</username>
            <password>${env.ARTIFACTORY_PASSWORD}</password>
        </server>
    </servers>
</settings>
